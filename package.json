{
  "name": "unity-entry",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "db.remove": "npm run db.remove && docker-compose rm",
    "db.stop": "cd ./db && docker-compose stop",
    "db.start": "cd ./db && docker-compose up -d",
    "db.init": "cd ./api && npm run db.init",
    "api.dev": "cd ./api && npm run start:dev",
    "ui.dev": "cd ./ui && npm run dev"
  }
}
